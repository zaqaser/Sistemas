palillo:array[5] of semaphore
(*palillo[i]=1)
 
 process type filosofo(n:integer);
 begin 
  repeat
  
  piensa;
   
  wait(palillo[n]);
  wait(palillo[(n mod 5)+1]);
  
  come;
  
  signal(palillo[n]);
  signal(palillo[(n mod 5)+1});
  forever
  end;
